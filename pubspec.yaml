name: valuatorx
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1
environment:
  sdk: ^3.7.0
dependencies:
  flutter:
    sdk: flutter
  flutter_dotenv: ^5.2.1
  flutter_map: ^8.1.1
  geolocator: ^11.0.0
  latlong2: ^0.9.1
  oauth2: ^2.0.1
  provider: ^6.1.4
  path_provider: ^2.1.5
  webview_flutter: ^4.0.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
flutter:
  uses-material-design: true
  assets:
    - assets/
    - .env
